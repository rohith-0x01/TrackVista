{
  "version": 2,
  "builds": [
    {
      "src": "server/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server/server.js" },
    { "src": "/(.+\\..+)", "dest": "server/public/$1" },
    { "src": "/(.*)", "dest": "server/server.js" }
  ]
}
